Your Goal is to scan the vulnerabilities and fix them.

Do the followings:

1. Run `npm audit` on the repo to find vulnerabilities installed packages and code in this project
2. Run `npm audit fix` to apply fix and updates
3. Run tests and make sure the updates didn't break anything in the project

